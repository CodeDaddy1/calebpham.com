## MDX is wired

This paragraph is rendered from `src/content/smoke.mdx` through `@next/mdx`. It proves the loader, the `mdx-components.tsx` mapping, and the prose styles before any real content exists. It is deleted in Phase 1.

```ts
const proof = 'MDX renders a fenced block in Geist Mono'
```
